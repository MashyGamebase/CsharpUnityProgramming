# CsharpUnityProgramming
This contains the Unity project, integrating the server mock package and making use of it to emulate a server-client authoring, request and packet payloads.

## Summary
It allows the user to emulate a server login flow and server side combat logic and server side request, with simulated latency and packet.
